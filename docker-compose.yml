version: "3.8"

services:
  url-monitor:
    build: .
    container_name: url-monitor
    restart: unless-stopped
    ports:
      - "80:3000"
    environment:
      - PORT=3000
      - BASE_URL=https://13.239.33.12/
    volumes:
      - screenshots:/app/screenshots

volumes:
  screenshots:
